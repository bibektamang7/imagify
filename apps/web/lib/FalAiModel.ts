import { BaseModel } from "./BaseModel";

class FalAIModel extends BaseModel {
	public async generateImage(prompt: string, tensorPath: string) {}
	public async trainModel(zipUrl: string, triggerWord: string) {}
}
